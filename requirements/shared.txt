# Shared dependencies — used by both salesagent and marketing analyzer.
# Per-product requirements pull this in via `-r shared.txt`.

# ── Web / API ────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4

# ── Config & validation ──────────────────────────────
pydantic==2.11.7
pydantic-settings==2.6.1

# ── HTTP client ──────────────────────────────────────
httpx==0.27.2

# ── Task queue ───────────────────────────────────────
# Celery + Redis are shared because both products run their own workers
# (separate queue names in combined mode); marketing will need scheduled
# ad-API polling from day one of Phase 1.
celery[redis]==5.4.0
redis==5.2.0
flower==2.0.1

# ── Database ─────────────────────────────────────────
supabase==2.28.3

# ── Observability ────────────────────────────────────
python-json-logger==2.0.7
sentry-sdk[fastapi,celery]==2.18.0
